apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pems-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pems-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pems-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
